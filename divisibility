#include <stdio.h>

int main(){
	int i;
	int N = 0;
	scanf("%d", &N);
	
	long data[N];
	for(auto i=0; i<N; i++)
	    scanf("%ld", &data[i]);
	char ans[3];
    // write your code here
    // ans = 
	 if (data[N-1] % 10 == 0)
        printf("Yes");
    else
        printf("No");
    
    return 0;
}
